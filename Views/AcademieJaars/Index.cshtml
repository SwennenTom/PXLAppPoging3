@model IEnumerable<PXLApp.Models.AcademieJaar>

@{
    ViewData["Title"] = "Academiejaren";
}

<h1>Academiejaren</h1>

<p>
    <a asp-action="Create"><img src="/images/plus.png" style="width: 35px;" alt="Nieuw" /></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StartDatum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.StartDatum)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.AcademieJaarId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AcademieJaarId">Details</a> |*@
                <a asp-action="Delete" asp-route-id="@item.AcademieJaarId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
